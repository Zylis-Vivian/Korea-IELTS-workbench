# 发音后端环境变量示例（复制为 .env 后填写真实值，不要提交 .env 到仓库）
# 优先级：Azure Neural TTS → Google Cloud TTS → 浏览器 Web Speech API
# 未配置的方案会被自动跳过；全部缺失时由前端降级到 Web Speech API。

# Azure Neural TTS（首选，女声 ko-KR-SunHiNeural / 男声 ko-KR-InJoonNeural）
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=eastasia

# Google Cloud TTS（备选，ko-KR-Neural2-A）
GOOGLE_TTS_API_KEY=

# 可选：后端端口，默认 8787
# PORT=8787
