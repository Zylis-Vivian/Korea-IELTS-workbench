# Lavender Study · 韩语+雅思双轨学习工作台

> 一个面向韩语初学者与雅思考生的双轨制在线学习平台，主打「标准首尔音发音」与「雅思全科训练」。

---

## 一、项目概述

| 项 | 内容 |
|----|------|
| 项目名称 | `lavender-study`（薰衣草学习台） |
| 定位 | 韩语入门 + 雅思备考一站式学习 |
| 主题色 | 薰衣草紫 `#B19CD9` |
| 项目路径 | `D:\workBuddy工作台\韩语+雅思工作台` |
| 线上地址 | https://9c5b19146c224f8aa58de7cb38d1a355.bj9.agentos-app.net |
| 首次发布 | 2026-07 部署于 CloudStudio 静态沙箱 |

**核心亮点**
- 🎧 **标准首尔音发音引擎**：所有韩语字母/单词均可点击发声，元音播纯元音、辅音播基础音（가/나/다）、收音播代表音（악/안/앍）。
- 📚 **双轨内容**：韩语（四十音、词汇、对话、语法、发音、文化、视频、教材）+ 雅思（听/说/读/写/词汇/语法/评分）。
- 📊 **学习管理**：仪表盘、打卡、看板、设置（含浅/深/跟随系统三主题）。

---

## 二、技术架构

| 层 | 技术 |
|----|------|
| 构建 | Vite 5.4 |
| 框架 | React 18 + TypeScript |
| 样式 | Tailwind CSS（薰衣草主题） |
| 状态 | Zustand |
| 路由 | React Router v6 |
| 动画 | Framer Motion |
| 图表 | Recharts |
| 文档解析 | PDF.js v4 |
| 语音 | Web Speech API + Edge TTS（浏览器原生）+ 预生成 MP3 |

**发音优先级链路**（点击 🔊 后的执行顺序）：
1. **本地预生成 MP3**（同域 `/audio/ko/<sha1>.mp3`，首选，无 CORS、零延迟）
2. 后端 `/api/tts`（Azure Neural / Google Cloud，需环境变量）
3. Edge TTS（keyless，`edge-tts-universal` 浏览器版，动态懒加载）
4. Web Speech API（`utterance.lang='ko-KR'`，cancel-then-speak 兜底）

> ⚠️ 历史坑：早期 `edge-tts-universal` 在模块加载时同步导入会触发 TDZ 导致整页白屏，已改为双层动态 `import()`；另修过 `manifestPromise` 变量 TDZ、`loadManifest` 顶层调用顺序问题。

---

## 三、功能模块

### 韩语学习（9 模块）
| 模块 | 路径 | 说明 |
|------|------|------|
| 四十音图 | `korean-alphabet` | 21 元音 + 19 辅音 + 27 收音，每个字符含主 🔊（基础音）+ 示例卡片 🔊（示例词） |
| 发音练习 | `korean-pronunciation` | 音变规则练习，页面加载自动检测并输出控制台报告 |
| 词汇学习 | `korean-vocab` | 分类词汇 + 发音 |
| 情景对话 | `korean-dialogue` | 日常对话场景 |
| 语法 | `korean-grammar` | 语法点讲解 |
| 专项练习 | `korean-practice` | 巩固训练 |
| 韩国文化 | `korean-culture` | 文化拓展 |
| 视频 | `korean-video` | 视听材料（含喜马拉雅专辑） |
| 教材中心 | `book-center` | 配套教材词汇 |

### 雅思学习（8 模块）
| 模块 | 路径 | 说明 |
|------|------|------|
| 词汇 | `ielts-vocab` | 雅思核心词 |
| 语法 | `ielts-grammar` | 写作/口语语法 |
| 听力 | `ielts-listening` | 听力训练 |
| 阅读 | `ielts-reading` | 阅读训练 |
| 口语 | `ielts-speaking` | 口语话题 |
| 写作 | `ielts-writing` | 写作批改/模板 |
| 评分标准 | `ielts-scoring` | 评分细则对照 |
| 分数计算 | `ielts-scorings` | 模考算分 |

### 通用模块
- `dashboard` 仪表盘 · `board` 学习看板 · `checkin` 打卡 · `settings` 设置（主题/偏好）

---

## 四、发音系统详解（重点）

### 4.1 四十音基础音映射（经真机全量检测 67/67 通过）
| 类别 | 主 🔊 播放内容 | 示例 |
|------|--------------|------|
| 元音（如 ㅓ） | 纯元音 **"어"** | 아/어/오/우… |
| 辅音（如 ㄱ） | 字母+ㅏ = **"가"** | ㄴ→나、ㄷ→다… |
| 收音（如 ㄱ） | 아+收音 = **"악"** | ㄴ→안、ㄺ→앍… |

### 4.2 音变预处理
- `applyPhoneticsIfNeeded(text)`：仅当文本含空格（即短语/句子）时才做跨词连音（如 `밥 먹어요`→`밤 머거요`），单字/字母原样返回。

### 4.3 预生成音频
- 生成脚本：`scripts/gen-audio.mjs`
- 输出：`public/audio/ko/<sha1前24位>.mp3` + `manifest.json`
- 规模：**503 条 manifest / 504 个文件**（含 21 元音纯音、19 辅音基础音、27 收音代表音、词汇/对话/语法/教材词等）
- 韩文合成：因兼容性字母（ㄱㄴㄷ 等 U+3131）不受 NFC 自动组合，使用 **Unicode 规范码点映射**（`0xAC00 + 初声×588 + 中声×28 + 终声`）手工组字。

---

## 五、目录结构

```
lavender-study/
├── index.html                 # 入口（Google Fonts 已改非阻塞异步加载）
├── vite.config.ts             # 构建配置
├── package.json
├── public/
│   └── audio/ko/              # 503 个 MP3 + manifest.json
├── scripts/
│   ├── gen-audio.mjs          # 发音音频批量生成
│   └── rebuild-manifest.mjs   # manifest 重建
├── src/
│   ├── App.tsx                # 路由与布局
│   ├── main.tsx               # 入口（含 AppErrorBoundary）
│   ├── components/            # SpeakerButton / AppErrorBoundary 等
│   ├── hooks/usePronunciation.ts   # 发音主逻辑（双层懒加载 edge-tts）
│   ├── utils/
│   │   ├── edgeTts.ts         # Edge TTS 封装（动态 import）
│   │   ├── koreanPhonetics.ts # 音变规则
│   │   └── pronunciationCache.ts
│   ├── data/                  # alphabet/pronunciation/vocab/dialogue/grammar/book-vocabulary/ielts* 等
│   ├── modules/               # 19 个功能模块
│   ├── stores/                # Zustand 状态
│   └── types.ts
└── dist/                      # 构建产物（部署用）
```

---

## 六、本地运行与构建

```bash
# 安装依赖
npm install

# 开发预览
npm run dev

# 重新生成发音音频（改了字母数据后必跑）
node scripts/gen-audio.mjs

# 生产构建（注意：dist 用 mv 改名而非 rm，避免触发安全删除钩子）
mv dist dist_old_$(date +%s) 2>/dev/null
npm run build

# 本地预览构建产物
npm run preview -- --port 4173
```

---

## 七、部署与访问

- **部署目标**：CloudStudio 静态沙箱（同沙箱多次更新）
- **线上地址**：https://9c5b19146c224f8aa58de7cb38d1a355.bj9.agentos-app.net
- **管理入口**：应用内「设置 → 数据管理 → 我发布的应用」可删除旧版本
- **访问建议**：首次打开按 **Ctrl+Shift+R** 硬刷新，清掉旧构建缓存

---

## 八、注意事项

1. **发音后端非必需**：云端无 Azure/Google 后端时，自动回退到本地 MP3（首选）与 Edge TTS，仍能正常发声；页面加载时的 `/api/tts` 502 为无害诊断噪音。
2. **D 盘路径**：所有源码与构建产物均在 `D:\workBuddy工作台\韩语+雅思工作台`，与历史 C 盘副本无关（旧副本因安全钩子保留未删）。
3. **主题**：全站支持浅色/深色/跟随系统三模式，切换即时无闪烁。
4. **已知盲区**：发音测试页的少量极偏收音（如 ㅆ/ㅌ 作单收音的非标准组合）未单独生成音频，不影响常规教学。
